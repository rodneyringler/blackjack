export class Card{

    constructor(rank, suit, numbervalue, imgfile) {
        this.rank = rank;
        this.suit = suit;
        this.numbervalue = numbervalue;
        this.imagefile = imgfile;
    }
}
